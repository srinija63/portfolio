export const EMAIL = 'saradasrinija@gmail.com';

/** Opens Gmail compose in the browser (works even without a desktop mail app). */
export const EMAIL_LINK = `https://mail.google.com/mail/?view=cm&fs=1&to=${encodeURIComponent(EMAIL)}&su=${encodeURIComponent('Hello from your portfolio')}`;
